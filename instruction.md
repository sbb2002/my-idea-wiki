# 아이디어 위키 — 사용 설명서

> 서버는 이미 Render에 배포된 상태를 가정합니다.

---

## 1. 최초 설정

### 1-1. 텔레그램 봇 만들기

1. 텔레그램에서 **@BotFather** 검색 → `/newbot` 명령 전송
2. 봇 이름과 username 입력 (username은 `_bot`으로 끝나야 함)
3. BotFather가 발급한 **봇 토큰** 복사 (예: `123456789:ABCdef...`)

**Chat ID 확인 방법:**

1. 방금 만든 봇에게 아무 메시지나 전송
2. 브라우저에서 아래 URL 접속 (토큰 교체)
   ```
   https://api.telegram.org/bot<봇토큰>/getUpdates
   ```
3. 응답 JSON에서 `"chat": {"id": 123456789}` 값이 Chat ID

---

### 1-2. Google Drive 폴더 설정

**폴더 구조:**
```
내 드라이브/
└── my-idea-wiki/
    ├── notes/        ← 여기에 아이디어 노트를 업로드
    └── wikis/        ← 위키 결과물이 저장됨 (건드리지 않아도 됨)
        └── pic/      ← PDF에서 추출한 그림이 저장됨
```

`scripts/setup_drive.py`를 실행하면 위 구조가 자동으로 생성됩니다.  
폴더 ID도 스크립트 실행 결과에서 바로 확인할 수 있습니다.

**폴더 ID 수동 확인 방법:**  
구글 드라이브에서 폴더를 열었을 때 URL의 마지막 부분이 폴더 ID입니다.
```
https://drive.google.com/drive/folders/1ABC123DEF456
                                        ↑ 이 부분
```

**⚠️ wikis 폴더에 빈 wiki.json 미리 업로드 (필수):**

서비스 계정은 구글 드라이브에 파일을 새로 생성할 수 없어요.
`wikis` 폴더에 아래 파일을 **직접 미리 업로드**해야 합니다.

> **업로드 전 필수 설정:** 구글 드라이브 웹 → 우측 상단 톱니바퀴(⚙️) → 설정 →
> **"업로드된 파일을 Google 문서 편집기 형식으로 변환" 체크 해제**

업로드할 파일:
- `wiki.json` — 아무 텍스트 에디터로 생성, 내용은 `{}` 또는 비워도 됨

> ℹ️ `index.html`은 더 이상 Drive에 저장하지 않습니다. GitHub Pages에 자동 배포됩니다.

**pic/ 폴더 공유 설정:**  
`wikis/pic/` 폴더는 뷰어에서 이미지를 인라인으로 불러오기 위해 **링크가 있는 모든 사용자에게 보기 권한** 으로 설정해야 합니다.

---

### 1-3. Render 환경변수 설정

개발자에게 아래 값들을 전달해서 Render 환경변수에 등록하면 설정 완료입니다:

| 항목 | 값 |
|------|-----|
| 텔레그램 봇 토큰 | BotFather에서 발급받은 토큰 |
| 텔레그램 Chat ID | 위에서 확인한 Chat ID |
| notes 폴더 ID | 구글 드라이브 notes 폴더 ID |
| wikis 폴더 ID | 구글 드라이브 wikis 폴더 ID |
| pic 폴더 ID | 구글 드라이브 wikis/pic/ 폴더 ID |
| GitHub PAT | `repo` 스코프로 발급한 Personal Access Token |

**GitHub PAT 발급 방법:**
1. [GitHub Settings → Tokens (classic)](https://github.com/settings/tokens) 접속
2. **Generate new token (classic)** 클릭
3. `repo` 스코프 체크 → **Generate token**
4. 발급된 토큰 복사 (한 번만 표시되므로 바로 저장)

---

## 2. 노트 작성법

### 지원 파일 형식

| 형식 | 처리 방식 |
|------|----------|
| `.txt` 텍스트 | 본문 그대로 위키화 |
| 구글 독스 | 드라이브에서 직접 읽어 위키화 |
| `.jpg` `.png` `.heic` 이미지 | Claude Vision으로 OCR 후 위키화 |
| `.pdf` | 페이지별 Vision 분석 — 텍스트·손글씨·그림 모두 추출 |

### 태그 사용
노트 본문 어디에나 `#태그명` 형식으로 태그를 달 수 있습니다.
태그가 있으면 AI 자동 분류보다 우선 적용됩니다.

```
오늘 RPG 전투 시스템에 대한 아이디어를 정리했다.

적의 약점을 분석해서 속성 공격을 하면 추가 대미지를 주는 방식으로...

#게임 #RPG #전투로직
```

### 기존 아이디어 추가/보완
이전에 위키화된 아이디어에 새 내용을 추가하고 싶으면,
**같은 주제로 새 노트를 작성해서 업로드**하면 됩니다.
AI가 기존 아이템을 인식하고 버전을 누적합니다.

### 코멘트 남기기
위키 아이템에 코멘트를 추가하려면 파일명 규칙을 따르는 텍스트 파일을 notes 폴더에 업로드합니다:

```
comment_<아이템ID>_<YYYY-MM-DD>.txt
예: comment_item_3a4b5c6d_2026-06-01.txt
```

아이템 ID는 뷰어의 인포박스에서 확인할 수 있습니다.
같은 날짜에 재업로드하면 기존 코멘트를 덮어씁니다.

---

## 3. 텔레그램 봇 사용법

봇에게 아래 명령어를 전송하세요. `/help`를 입력하면 봇이 직접 안내합니다.

| 명령어 | 동작 |
|--------|------|
| `/run` | 즉시 위키화 실행 (신규/변경 노트만) |
| `/rerun` | 전체 재처리 (노트가 스킵됐거나 결과 이상할 때) |
| `/status` | 실행 중이면 진행 중 안내, 완료됐으면 마지막 결과 조회 |
| `/schedule` | 현재 자동 실행 주기 확인 |
| `/set <주기>` | 자동 실행 주기 변경 (메모용) |
| `/help` | 명령어 사용법 안내 |

**`/run` 완료 시 알람 예시:**
```
✅ [2026-06-01] 위키화 완료
• 처리: 5개 노트
• 신규: 2개 / 업데이트: 3개
• 사용 API: claude
• 뷰어: https://drive.google.com/drive/folders/...

📄 뷰어에서 결과를 확인하세요:
https://drive.google.com/drive/folders/...
```

**알람 종류:**

| 아이콘 | 의미 |
|--------|------|
| ✅ | 위키화 완료 — 처리/신규/업데이트 수 요약 + GitHub Pages 링크 |
| ⚠️ | 일부 실패 — 성공한 부분 결과와 함께 안내 + GitHub Pages 링크 |
| ❌ | 전체 실패 — Claude, Gemini 모두 실패 |
| ⏳ | 서버 슬립 중 — `/run` 후 콜드 스타트 대기 안내 |

> **콜드 스타트 안내:** 텔레그램 Webhook 서버는 15분 이상 미사용 시 슬립 상태가 됩니다.
> 슬립 중 `/run`을 보내면 "잠시만 기다려주세요" 메시지가 오고 30초~1분 후 작업이 시작됩니다.

---

## 4. 결과 확인 — 위키 뷰어 열기

위키화가 완료되면 **GitHub Pages**에 자동으로 배포됩니다.

### 접근 방법

**URL로 바로 접근 (권장)**
- 브라우저에서 `https://sbb2002.github.io/my-idea-wiki/` 접속
- PC · 모바일 모두 지원, 북마크에 등록해두면 편리

**완료 알람으로 접근**
- `/run` 완료 알람의 GitHub Pages 링크 클릭

> 📌 구글 드라이브에서 `index.html`을 다운로드할 필요가 없어졌습니다.
> `wiki.json`은 Drive에 백업으로 저장되지만, 뷰어는 GitHub Pages가 정식 경로입니다.

**`/run` 완료 시 알람 예시:**
```
✅ [2026-06-01] 위키화 완료
• 처리: 5개 노트
• 신규: 2개 / 업데이트: 3개
• 사용 API: claude
• 뷰어: https://sbb2002.github.io/my-idea-wiki/

📄 뷰어에서 결과를 확인하세요:
https://sbb2002.github.io/my-idea-wiki/
```

---

## 5. 위키 뷰어 UI 안내

### 화면 구성

**데스크탑 (900px 이상)**
```
┌─────────────────────────────────────────────────────┐
│ 💡 아이디어 위키  [검색창]  [위키|그래프]  [🌙다크]  │  ← 헤더
├──────────────┬──────────────────────────┬───────────┤
│              │  아이템 제목             │  목차     │
│  아이템 목록  │  ─────────────────────  │  · 개요   │
│              │  [인포박스]  개요 텍스트  │  · 히스토리│
│  · 아이템 A  │                          │  · 첨부   │
│  · 아이템 B  │  버전 히스토리           │  · 연관   │
│  · 아이템 C  │  이미지 첨부             │           │
│              │  연관 아이템             │           │
└──────────────┴──────────────────────────┴───────────┘
```

**모바일 (640px 이하)**
```
┌────────────────────────┐
│ ☰  💡아이디어위키  🌙  │  ← 헤더 (햄버거 + 테마)
├────────────────────────┤
│  ▼ 목차 (터치로 펼치기) │
├────────────────────────┤
│  아이템 제목            │
│  [인포박스 — 전체 너비] │
│  개요 텍스트            │
│  버전 히스토리          │
│  이미지 첨부            │
│  연관 아이템            │
├────────────────────────┤
│ ☰목록 📄위키 🕸️그래프 🌙│  ← 하단 탭바
└────────────────────────┘
```

### 주요 기능

**아이템 목록 (왼쪽 사이드바 / 모바일 드로어)**
- 전체 아이템이 목록으로 표시
- 클릭하면 해당 위키 문서로 이동
- **모바일**: 하단 탭바 **☰ 목록** 탭 또는 헤더의 **☰** 버튼으로 열기. 열린 상태에서 아이템 선택 시 드로어 자동 닫힘

**검색**
- 헤더의 검색창에 입력하면 제목·태그·요약에서 실시간 필터링

**목차 (TOC)**
- 데스크탑: 우측에 고정 표시
- 모바일: 문서 상단의 **▼ 목차** 버튼으로 접힘/펼침

**위키 문서 본문**
- **인포박스**: 요약 박스 (태그, 버전 수, 최신 날짜, 연관 아이템). 모바일에서는 전체 너비로 표시
- **개요**: AI가 작성한 아이디어 현재 상태 요약
- **버전 히스토리**: 타임라인 형식으로 아이디어 발전 과정 표시
- **이미지 첨부**: OCR 이미지 및 PDF에서 추출한 그림을 인라인으로 표시. 클릭하면 원본 이미지를 새 탭에서 열기
- **연관 아이템**: 클릭하면 해당 아이템으로 바로 이동

**그래프 뷰**
- 데스크탑: 헤더의 **[그래프]** 버튼으로 전환
- 모바일: 하단 탭바 **🕸️ 그래프** 탭으로 전환
- 아이템 간 연관관계를 노드-엣지 그래프로 시각화
- 노드 클릭(탭) → 해당 위키 문서로 이동
- 드래그로 노드 이동, 스크롤(PC) / 핀치줌(모바일)으로 줌 인/아웃
- 우측 하단 `+` `-` `⌂` 버튼으로 줌 조절 및 초기화

**테마 전환**
- 데스크탑: 헤더 우측 **[🌙 다크]** / **[☀️ 라이트]** 버튼
- 모바일: 하단 탭바 **🌙 테마** 탭
- 선택한 테마는 다음에 열어도 유지됨
